Побудова B+ дерева. Перевірка чи є конкретний елемент у побудованому дереві.
Для побудови дерева у функції main треба додавати по одному елементу 'x' за допомогою node.insert(x); Також на початку коду треба задати степінь.
В кінці main треба у node.search(); записати шукане число для перевірки його присутності в дереві.
Після побудови відобразиться дерево, а в наступному рядку Found чи Not found.
